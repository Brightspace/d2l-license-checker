engine-strict=true # only allow use of supported node versions
ignore-scripts=true # we don't rely on any install scripts so this is safer
yes=false # prevent npm/npx from auto answering yes to prompts which is safer
